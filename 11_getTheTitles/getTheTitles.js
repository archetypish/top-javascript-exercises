const getTheTitles = function (books) {
  // from an array of objects of books, get the titles only into an array

  // array of objects with titles and author
  // array of titles

  return books.map((book) => book.title);
};

// Do not edit below this line
module.exports = getTheTitles;
